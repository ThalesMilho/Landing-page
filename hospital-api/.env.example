# ═══════════════════════════════════════════════════════════════
# HOSPITAL INTRANET API — Environment Variables
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in real values.
# NEVER commit .env to source control.
# ═══════════════════════════════════════════════════════════════

# ── Server ──────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development                        # development | production

# ── Azure Active Directory ───────────────────────────────────────
# From: Azure Portal → Azure AD → App Registrations → Your App
AZURE_TENANT_ID=your-tenant-id-here        # e.g. a1b2c3d4-e5f6-7890-abcd-ef1234567890
AZURE_CLIENT_ID=your-client-id-here        # e.g. b2c3d4e5-f6a7-8901-bcde-f12345678901

# The audience the token was issued for — usually same as CLIENT_ID
# For multi-resource apps, it may be: api://your-client-id
AZURE_AUDIENCE=your-client-id-here

# ── CORS ─────────────────────────────────────────────────────────
# Frontend origin — NO trailing slash
ALLOWED_ORIGIN=http://localhost:5173       # In prod: https://intranet.hospital.com

# ── Rate Limiting ────────────────────────────────────────────────
# Max requests per window per IP
RATE_LIMIT_WINDOW_MS=900000                # 15 minutes
RATE_LIMIT_MAX=200                         # 200 requests per window

# ── Logging ──────────────────────────────────────────────────────
LOG_LEVEL=info                             # trace | debug | info | warn | error | fatal
